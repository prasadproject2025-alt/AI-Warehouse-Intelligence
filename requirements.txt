# Core computer vision & deep learning
ultralytics>=8.3.0
torch>=2.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Open-vocabulary detection (YOLO-World text encoder).
# Without this the system falls back to COCO YOLOv8, which cannot detect
# cartons, pallets or trolleys at all.
ftfy>=6.1.0
clip @ git+https://github.com/ultralytics/CLIP.git

# Backend & API
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Tests
pytest>=8.0.0
httpx>=0.27.0
